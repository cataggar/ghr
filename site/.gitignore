# Zig build artifacts (also covered by the repo-root .gitignore, listed
# here too since this directory is a self-contained mer/Zig project).
.zig-cache/
zig-out/
zig-pkg/
dist/

# Generated by `zig build codegen` — regenerated on every build.
src/generated/*
!src/generated/.gitkeep
